# 技術書翻訳ビューワー 設計書

英語の技術書をスムーズに読むためのローカルWebアプリ。左にPDF、右に翻訳を表示し、開いたページをオンデマンドで翻訳・キャッシュする。複数回の検討を経て、「高度な学習支援機能」を削ぎ落とし、コア機能だけに絞った最小構成（MVP）として定義する。

---

## 1. このアプリの目的と方針

技術書の英語を読む際の「翻訳する手間」をなくし、読書の流れを切らずに原文と訳を並べて読めるようにする。現時点では作者本人専用。

設計の指針は次の3つ。

第一に、**全文を一気に翻訳しない**。開いたページ、または翻訳ボタンを押したタイミングでのみ翻訳を生成する。これによりトークン消費とコストを抑え、精度も保つ。

第二に、**一度翻訳した結果はキャッシュして再利用する**。同じページを再表示するときはAPIを叩かず、ローカルDBから即座に読み込む。

第三に、**最小構成から始める**。図表のVision解説や矩形選択といった重い機能は、当面アプリに実装しない（後述の「やらないこと」を参照）。動くものを一本道で完成させ、読書しながら必要を感じた機能だけを後から足す。

---

## 2. スコープ

### やること（MVP）

左ペインにPDFを表示し、現在ページを検知する。右ペインにそのページの翻訳をMarkdownで表示する。翻訳結果はローカルにキャッシュし、再表示時はキャッシュから読む。ページまたぎの文章にも対応するため、前後数行を文脈としてAIに渡す。翻訳結果は蓄積され、読み終わる頃には自分専用の読書ノートになる。

### やらないこと（当面）

以下は使用頻度が低いと判断し、アプリには実装しない。

- **図表（Diagram）のVision解説**：たまにしか発生しないため、スクリーンショットを撮って別ウィンドウの通常のChatGPTに貼って質問する運用で代替する。アプリ内にVision経路・矩形選択UI・画像処理を持たない。
- **D&D矩形（四角形）選択による構造解析**：上記と同じ理由で実装しない。

### 検討中（MVP後に追加し得る）

- **テキスト選択によるピンポイント解説**：選択した文字列を渡すだけなので実装コストが翻訳とほぼ同じで、体験価値が高い。Visionと違い画像処理が不要。MVP完成後の最初の追加候補とする。詳細は「7. 拡張余地」参照。

---

## 3. AIプロバイダーの選定理由

翻訳の生成にはClaude（Anthropic）のMessages APIを直接利用する。

### なぜClaudeか

作者は2026年6月15日開始の月次クレジット枠（Proプランで月$20分のAgent SDK利用枠）を保有見込みだが、現在この種のプログラム利用をしていないため、放置すると毎月枠が失効する。このアプリで枠を活用すれば追加負担ゼロで価値を回収できる。

### なぜ枠の超過を心配しなくてよいか

作者の読書ペース（翻訳が必要な英語の技術書は月1冊未満）と本アプリの省トークン設計（オンデマンド＋キャッシュ）を前提にコストを試算した結果、月の消費は$20枠に十分収まる見込み（詳細は「6. コスト試算」）。「超過したら課金される」というクレジット制の弱点が、この使用量では実質的に問題にならない。

### なぜAgent SDKではなくMessages API直叩きか

翻訳は「短いテキストを渡して結果を受け取る」ステートレスな単発推論。エージェント基盤（会話の永続化やツール実行を前提とする構造）は不要で、素のMessages APIのほうが用途に素直。作者はAnthropic API直叩きの経験があり、実装も容易。

### 補足：クレジット枠開始前の扱い

2026年6月15日より前はAPIキーによる従量課金となるが、1冊あたり数ドル規模のため、開始を待たずに着手しても費用負担はごく小さい。

---

## 4. 機能仕様

### 4.1 UI構成（左右2ペイン）

**左ペイン（PDFビューア）**：`pdf.js` 等でPDFを表示する。現在開いているページ番号（`currentPage`）をリアルタイムに検知する。

**右ペイン（翻訳表示）**：Markdownレンダラー。キャッシュまたはAIが生成した翻訳を表示する。

### 4.2 オンデマンド翻訳とキャッシュ

ページを開いた時、または翻訳ボタンを押した時にのみ翻訳を生成する。2回目以降の表示ではAPIを叩かず、ローカルDB（SQLiteまたはJSON）から即座に読み込む。

**キャッシュキーの設計**：`PDFの内容ハッシュ + ページ番号 + プロンプトのバージョン` の3要素を組み合わせる。

- **PDFの内容ハッシュ（SHA-256等）**：ファイル名は同一性の保証にならない（リネームや同名異本で誤ヒットする）ため、内容そのもののハッシュをキーにする。
- **プロンプトのバージョン**：翻訳の指示文を改善したのに古いキャッシュが返る、という事故を防ぐためキーに含める。

### 4.3 ページまたぎへの文脈対策

1ページ単位で区切ると文末・文頭が途切れて翻訳精度が落ちる。これを防ぐため、翻訳リクエスト時に対象ページの前後数行を「文脈」としてAIに渡し、出力は指定したページ部分のみに限定するようプロンプトで指示する。

**既知の整合性メモ**：この方式では「キャッシュの単位（ページ）」と「生成時に参照する範囲（前後を含む）」がズレる。例えば5ページ目の訳は4ページ末と6ページ頭を文脈に含んで生成されるため、後から隣接ページを再生成した場合の整合は厳密には保証されない。実害は小さいと判断するが、仕様として認識しておく。

### 4.4 PDFノイズの除去

テキスト抽出時に混入するヘッダー・フッター（章タイトル、ページ番号）を除去する。**除去はAIに渡す前にプログラム側で行う**（正規表現、または `pdf.js` が返す座標情報を使ったページ端テキストの除去）。プロンプトで「無視して」と頼む方式は信頼性が低いため、前処理で確実に落とす。

### 4.5 読書ノートの蓄積

右ペインの翻訳データをMarkdown形式でローカルに蓄積する。読み終える頃には、訳と気づきが残った自分専用の備忘録ノートが自動生成される。これは翻訳アプリが学習成果物を生むツールへ昇格する、本アプリの中心的な価値。

---

## 5. アプリケーション形態・技術スタック・動作環境

### 動作環境

個人PC（Mac）と社用PC（Windows）の両方で使う。データ同期は不要（各環境で独立して動けばよい）。

### アプリ形態：ローカルWebアプリ（ローカルサーバー方式）

TypeScript で実装する。Node.js でローカルサーバーを立て、ブラウザで `http://localhost:<port>` を開いて使う「ローカルサーバー方式」を採る。HTMLファイルを `file://` で直接開く方式は、ブラウザのセキュリティでローカルPDF/ファイルの読み書きが制限されるため採らない。

**この形態を選んだ理由（配布とadmin権限）**：本アプリは両環境で使うが、社用Windowsは管理者権限が制限される前提。デスクトップアプリ（Tauri等）は各OS向けにネイティブバイナリ（`.exe`）をビルドする工程が必要で、ビルドツールチェーンの導入が管理者権限を要求しがちで摩擦が大きい。対してローカルWebは、TSコードがMac/Windowsとも同じ `node` で動くため、プラットフォーム別ビルドが存在しない。Windowsへは「コードをコピーして `npm install` → 起動」だけで配布が完結する。Node.js は社用PCに導入済みで利用可能なことを確認済み。

**トレードオフ**：起動が「サーバー起動 → ブラウザで localhost を開く」の2段階になる（`.exe` ダブルクリックより手間）。自分専用ツールのため許容範囲。起動用のスクリプト／バッチを用意すれば、ダブルクリック起動程度には簡略化できる。

### 構成（フロントエンド／バックエンドの分離）

ローカルサーバー方式の要点は、**APIキーをブラウザ側に出さない**こと。ブラウザのJSにキーを直書きすると開発者ツールで露見するため、キーはサーバー（Node）側だけが保持する。

- **ブラウザ（フロントエンド）**：PDF表示（`pdf.js`）、現在ページ検知、右ペインのMarkdown表示、翻訳リクエストの送信。AIへは直接アクセスせず、ローカルサーバーを経由する。
- **ローカルサーバー（Node + TS）**：APIキーを保持し、ブラウザからの依頼を受けて Claude Messages API を呼ぶ。ローカルDB（キャッシュ／ノート）の読み書きもここで行う。

リクエストの流れ：ブラウザ「このページを訳して」→ ローカルサーバーがキャッシュを確認 → 無ければ Claude API を呼ぶ → 結果をDBに保存しつつブラウザへ返す。APIキーは一度もブラウザに渡らない。

### 想定スタック

- **言語**：TypeScript（フロント／サーバー共通）
- **サーバー**：Node.js
- **PDF表示**：`pdf.js`（ブラウザ側）
- **UI**：Webフロントエンド（右ペインはMarkdownレンダラー）
- **キャッシュ／ノート保存**：SQLite または JSON によるローカル保存（サーバー側）
- **AI呼び出し**：Anthropic Messages API（Claude）直叩き（サーバー側）

### 環境上の注意

Claudeの認証はAPIキー（または6月15日以降はクレジット枠）を用い、サーバー側に保持する。APIキー方式のため、OAuthのlocalhostコールバック問題（ブラウザとローカルサーバのlocalhostが食い違う事象）は発生しない。将来OAuth系の認証を持つプロバイダーを足す場合のみ、各環境での認証経路を実機確認すること。

社用PCでの利用は、`.exe` 等のネイティブ実行ファイルを必要としない（`node` でスクリプトを動かすだけ）。導入物はプロジェクトのソースと npm 依存パッケージのみで、いずれもユーザー領域で完結する。

---

## 6. コスト試算

モデルは翻訳用途の標準として **Claude Sonnet 4.6（入力 $3 / 出力 $15、各100万トークンあたり）** を想定。クレジット枠は正規APIレートで消費されるため、この単価で計算する。

### 1ページあたり

文脈付与（前後数行）を織り込んだ現実寄りの見積もりで、**1ページあたり約 $0.027（約4円）**。コストの約9割は出力（日本語訳）側が占める。

### 1冊あたり（読みそうな定番書のページ数で）

| 書名 | ページ数 | 1冊コスト | $20で何冊 |
|---|---|---|---|
| A Philosophy of Software Design (2nd) | 約210 | 約 $5.7 | 約3.5冊 |
| Clean Architecture 級 | 約350 | 約 $9.5 | 約2.1冊 |
| Designing Data-Intensive Applications 級（最厚） | 約600 | 約 $16.3 | 約1.2冊 |

### 実使用量の結論

作者の実ペース（翻訳が必要な英語技術書は月1冊未満、詰まったページだけ翻訳）では、月の消費は$20枠の数割にとどまる見込み。最も分厚いDDIA級を1冊まるごと訳しても枠内に収まる。**$20クレジット枠は実使用上、十分な余裕がある。**

### コストをさらに下げられる手（必要になれば）

- **キャッシュ**：一度訳したページの再表示は無料（API不使用）。設計に既に組込済み。
- **モデル使い分け**：本文のベタ翻訳をHaiku 4.5（$1/$5）に回すと1ページ単価が半分以下になる。難所だけ上位モデルへ。
- **Batch API（50%引き）**：即時応答ではない代わりに半額。「今読むページは通常API、先のページを先回りでバッチ翻訳」のハイブリッドにすればコストとUXを両立できる。
- 補足：プロンプトキャッシュは入力側にしか効かず、本用途はコストの大半が出力のため恩恵が小さい。

---

## 7. 拡張余地（MVP後）

MVP（純粋な翻訳ビューワー）を完成させた後、読書しながら必要を感じたものだけを足す。実装を将来分けやすくするため、AI呼び出しは「プロバイダー層」1枚にまとめ、内部を `translateText()` のような関数単位に分けておく。これにより後からプロバイダーの差し替えや機能追加が局所的に済む。

### 候補1：テキスト選択によるピンポイント解説（有力）

選択した単語や1〜2文について、翻訳ではなく文脈に応じた「解説」を生成する。`Information Hiding` `Deep Module` のような重要概念の噛み砕きや、`tactical vs strategic` のような対比表現のニュアンス補足に使う。

選択文字列を渡すだけで画像処理が不要なため、実装コストは翻訳とほぼ同じ（同じMessages APIにプロンプトを変えて投げるだけ）。別ウィンドウへ移動せず読書の流れを保てるため、体験価値が高い。**MVP後の最初の追加候補。**

### 候補2：図表のVision解説（当面は外部運用）

モジュール境界図やクラス依存図の解説。使用頻度が低いため当面アプリには実装せず、スクリーンショット＋通常ChatGPTで代替する。将来、もしVisionの利用が増えコストやUXに不満が出てきたら、その時に初めて実装を検討する。その際は、本文翻訳（軽い・高頻度）と図表解説（重い・低頻度）を別プロバイダーに振り分ける案（例：図表解説をChatGPTサブスク側に寄せる）も選択肢になるが、二重実装の複雑さを伴うため、必要がデータで確認できてから判断する。

---

## 8. 設計上の決定事項まとめ

| 論点 | 決定 | 理由 |
|---|---|---|
| アプリ形態 | ローカルWebアプリ（Node + TS、ブラウザでlocalhost） | プラットフォーム別ビルドが不要で、admin権限の要らない配布が可能 |
| 配布方法 | コードをコピーして `npm install` → 起動 | `.exe`ビルド・署名・admin権限が一切不要。Node導入済みを確認済み |
| APIキーの保持 | サーバー（Node）側のみ。ブラウザには出さない | ブラウザ直書きは開発者ツールで露見するため |
| AIプロバイダー | Claude（Messages API直叩き） | 失効する$20枠の活用、ステートレス用途に素直、実装経験あり |
| 翻訳のタイミング | オンデマンド（ページ表示/ボタン押下時） | トークン・コスト節約、精度維持 |
| キャッシュキー | 内容ハッシュ＋ページ＋プロンプト版 | ファイル名は同一性を保証しない／指示文改善時の誤キャッシュ防止 |
| ノイズ除去 | AIに渡す前にプログラム側で除去 | プロンプト指示は信頼性が低い |
| 図表Vision解説 | アプリに実装しない（外部運用） | 低頻度のため費用対効果が逆ザヤ |
| テキスト選択解説 | MVPでは見送り、最初の拡張候補 | 軽いが、まず最小構成を完成させる |
| 開発の進め方 | 最小構成を一本道で完成 → 必要な機能だけ追加 | ソロ開発で完成と継続のしやすさを優先 |