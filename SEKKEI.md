# 技術書翻訳・学習支援アプリケーション仕様・設計メモ

本書籍『A Philosophy of Software Design』の読書体験を最大化するため、AI（Codex app-server等）を活用した左右2ペイン構成のインタラクティブなローカルWebアプリケーションの仕様・設計のまとめです。

---

## 1. 概算トークン数とコスト見積もり
書籍『A Philosophy of Software Design』を一冊丸ごと翻訳した場合の消費トークン数の目安です。

* **原文（英語）：** 約 45,000 〜 50,000 語 ≒ **約 65,000 トークン**
* **翻訳文（日本語）：** 英語の約3〜4倍に膨らむため ≒ **約 250,000 トークン**
* **その他（プロンプト・重複文脈）：** **約 30,000 〜 50,000 トークン**
* **総消費トークン数（目安）：** **約 350,000 〜 50,0000 トークン（35万〜50万トークン）**

---

## 2. アプリケーション概要と主要機能

### UI構成
* **左ペイン（PDF表示）：** `pdf.js` 等を使用したPDFビューア。現在開いているページ番号（`currentPage`）をリアルタイムに検知する。
* **右ペイン（翻訳・解説表示）：** Markdownレンダラー。ローカルDBから取得した、またはAIによって生成された翻訳・解説コンテンツを表示する。

### コスト・制限対策（オンデマンド生成 & キャッシュ）
* **オンデマンド生成：** コンテキスト窓の無駄遣いやAPI制限、コストを抑えるため、開いたページ、または「翻訳ボタン」を明示的に押したタイミングのみ翻訳を出力する。
* **ローカルキャッシュ：** 「PDF名 + ページ番号」をキーとして、翻訳結果をローカルのデータベース（SQLiteやJSON等）に保存。2回目以降の表示ではAPIを叩かず、瞬時にキャッシュから読み込む。

---

## 3. 高度な学習支援機能

### ① テキスト選択（ハイライト）によるピンポイント解説
PDF内の特定の単語や1〜2文をユーザーが選択した際、翻訳だけでなくコンテキストに応じた「解説」を生成・表示する。
* **重要コンセプトの深掘り：** `Information Hiding` や `Deep Module` など、本書の核心となる専門用語を噛み砕いて要約。
* **英語のニュアンス解説：** 技術書特有の対比表現（`tactical` vs `strategic` など）の意図を補足。

### ② D&D矩形（四角形）選択による構造解析
画面上をドラッグしてエリア指定（スクショ的な認識）し、AIのVision API等を活用して解説を生成する。
* **コードブロックの解説：** 本書に登場する「悪いコード例」「良いコード例」の挙動や、なぜそれが複雑性を生むのかをステップバイステップで解説。
* **図表（Diagram）の解説：** モジュールの境界線やクラスの依存関係を表した図を認識させ、設計上のメリット・デメリットを視覚情報から解説。

---

## 4. 実装・運用における考慮事項

* **ページまたぎ文章へのコンテキスト対策：** 1ページ単位で区切ると文末・文頭が途切れて翻訳精度が落ちるため、翻訳リクエスト時に前後の数行を「文脈（コンテキスト）」としてAIに渡し、指示したページ部分のみを出力させる。
* **PDFノイズの除去：** テキスト抽出時に混入するヘッダー・フッター（章タイトルやページ番号）を、正規表現やプロンプト指示でクレンジングする。
* **読書ノートとしての蓄積：** 右ペインの解説データやハイライト箇所をMarkdown形式でローカルに蓄積していくことで、読み終える頃には**自分だけの『A Philosophy of Software Design』攻略・備忘録ノート**が自動生成される。